# LearnPy

Le projet applicatif est dans le dossier `ddaw-learnpy/`.

## Lancement

```bash
cd ddaw-learnpy
docker compose up --build
```

Le README complet, le backend, la configuration Docker Compose et le seed SQL sont centralisés dans `ddaw-learnpy/`.
